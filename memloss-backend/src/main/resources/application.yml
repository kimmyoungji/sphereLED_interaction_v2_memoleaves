server:
  port: 8080

spring:
  main:
    web-application-type: reactive

management:
  endpoints.web.exposure.include: health,info
  endpoint.health.probes.enabled: true

capacity:
  maxSessions: 4
  heartbeatIntervalSeconds: 30
  staleGraceSeconds: 90
  cleanupIntervalMs: 30000
